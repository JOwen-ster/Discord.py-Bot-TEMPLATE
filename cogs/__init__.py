# All extensions (cog filenames) that wil be loaded
names = [
    # Main Cogs
    "message",
    
]

# Used to tell how many cogs are ready (according to @commands.Cog.listener())
# cog_counter will be a max of len(names)
cog_counter = 0